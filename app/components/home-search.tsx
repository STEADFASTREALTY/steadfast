"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function HomeSearch({ locations }: { locations: string[] }) { const [location, setLocation] = useState(""); const [buy, setBuy] = useState(true); const [rent, setRent] = useState(false); const router = useRouter(); const submit = (event: React.FormEvent) => { event.preventDefault(); const params = new URLSearchParams(); if (location) params.set("location", location); params.set("intent", rent && !buy ? "rent" : "buy"); router.push(`/properties?${params.toString()}`); }; return <form className="home-search" onSubmit={submit}><label><span>City or area</span><select value={location} onChange={(event) => setLocation(event.target.value)}><option value="">Any city or area</option>{locations.map((city) => <option key={city} value={city}>{city}</option>)}</select></label><div className="home-search-intents"><label><input type="checkbox" checked={buy} onChange={(event) => setBuy(event.target.checked)} /> Buy</label><label><input type="checkbox" checked={rent} onChange={(event) => setRent(event.target.checked)} /> Rent</label></div><button type="submit">Search properties</button></form>; }
